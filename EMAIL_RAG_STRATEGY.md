# EML 邮件数据作为 RAG 知识库的可行性分析与策略建议

> 基于当前项目现状（**7000 封原始邮件** + 服装术语 RAG + 邮件微调流水线）的深度分析

---

## 一、当前现状诊断

### 1.1 已有资产
- **术语 RAG**：基于《汉英英汉服装分类词汇》PDF，约 12,000 条结构化术语，使用 BGE-large-zh + ChromaDB
- **邮件微调流水线**：**7000 个 `.eml` 文件** → 去重/筛选/脱敏/结构化提取 → 预计 3000~5000 条有效业务事件记录
- **集成模块**：`EmailTerminologyAssistant` 可在邮件中自动识别并翻译专业术语

### 1.2 数据质量问题（抽样发现）
| 问题 | 具体表现 | 影响 |
|------|---------|------|
| **编码/乱码** | `Ͽȷϣ밲ŷ`、`ɫƷʶͬԭ¡` 等乱码残留 | 直接入向量库会污染检索质量 |
| **结构嘈杂** | 大量签名块、转发头、邮件头、纯数字表格 | 有效信息密度低，信噪比差 |
| **数据量充足** | 原始 **7000 封** → 预估去重筛选后有效记录 **3500+ 封** | 足以支撑业务知识库，但自动化清洗和结构化提取成为瓶颈 |
| **时效混杂** | 邮件包含不同时间的交期、价格、流程 | 可能给出过时答案 |

### 1.3 核心结论
> **技术上可行，但不宜直接将处理后的 `.eml` 数据灌入现有的术语 RAG 中。** 两者在数据形态、检索精度要求、答案生成方式上差异很大。

---

## 二、将 .eml 作为 RAG 知识库的利弊分析

### ✅ 优势（Why do it）

| 优势 | 说明 |
|------|------|
| **业务经验沉淀** | 术语 RAG 只能回答"拉链英文是什么"；邮件 RAG 能回答"ASOS 核料流程一般多久"、"Lab Dip 确认后下一步做什么" |
| **动态更新** | 新邮件持续产生，可增量更新向量库，比重新微调模型灵活得多 |
| **降低幻觉** | 基于真实业务往来生成答案，比让模型纯靠参数记忆更可靠 |
| **隐私可控** | 现有 pipeline 已完成脱敏（邮箱、电话、人名、订单号），可直接入库 |

### ❌ 劣势（Risks & Costs）

| 劣势 | 说明 |
|------|------|
| **信噪比极低** | 签名、转发链、HTML 残留、表格数字会严重干扰语义检索 |
| **检索粒度粗** | 一封邮件往往同时涉及订单、面料、交期多个主题，固定长度切块会破坏上下文 |
| **自动化瓶颈** | 7000 封邮件无法人工逐条清洗，必须依赖自动化流水线 + LLM 结构化提取，提取准确率成为关键 |
| **时效性陷阱** | 旧邮件中的价格、交期、流程可能已经过时，RAG 不加时间过滤会给出错误建议 |
| **稀释术语检索** | 若混合存入现有术语库，术语查询的精准度会被业务邮件噪声拉低 |

---

## 三、典型使用场景示例：款号延期根因追踪

> 以下内容用于锚定所有技术设计的核心目标。

### 3.1 用户原始提问

> **"款号 INDOT27551，工厂要求延期，帮我查一下是哪个延期导致的。"**
> 
> **面料部分**：提交 Lab Dip / 手刮时间、客人确认时间、确认回工厂时间、工厂给大货样时间、客人确认大货样时间、工厂出货时间。
> 
> **样衣部分**：客人给每个阶段的意见时间、工厂提交样衣时间、封样时间。

### 3.2 场景特征分析

| 特征 | 说明 | 对系统的挑战 |
|------|------|-------------|
| **跨邮件查询** | 一个款号的时间线可能分散在 5~20 封往来邮件中 | 需要按 `style_id` 聚合，而不是按单封邮件检索 |
| **结构化推理** | 需要对比"计划时间"vs"实际时间"，找出最大延误点 | 模型需要基于检索结果做表格化对比和根因分析 |
| **多阶段覆盖** | 涉及 Lab Dip、大货样、样衣、封样、出货等多个阶段 | 元数据必须精确到 `category` 和 `event_type` |
| **不可编造** | 如果某阶段邮件缺失，必须明确告知"未找到记录" | 模型必须学会"基于证据回答"，不能 hallucinate |

### 3.3 理想输出示例

```markdown
## INDOT27551 时间线梳理与延期根因分析

### 面料部分

| 阶段 | 事件 | 实际时间 | 计划时间 | 延期 |
|------|------|---------|---------|------|
| Lab Dip | 工厂提交 | 5/10 | 5/08 | +2天 |
| Lab Dip | 客人确认 | 5/15 | 5/12 | +3天 |
| Lab Dip | 确认回工厂 | 5/16 | - | - |
| 大货样 | 工厂提交 | 6/01 | - | - |
| 大货样 | 客人确认 | 6/05 | - | - |
| 出货 | 工厂出货 | 7/05 | 原 6/25 | +10天 |

### 样衣部分

| 阶段 | 事件 | 时间 | 备注 |
|------|------|------|------|
| 样衣提交 | - | 未找到记录 | - |
| 封样 | - | 未找到记录 | - |

### 根因结论
根据邮件记录，本次延期的关键瓶颈在 **Lab Dip 阶段**：
1. 工厂提交 Lab Dip 比计划晚 2 天（5/10 vs 5/08）
2. 客人确认 Lab Dip 晚 3 天（5/15 vs 5/12）

这两处延误直接导致后续大货样和出货时间整体后移。工厂于 6/20 申请将出货日从 6/25 延至 7/05。
```

---

## 四、推荐架构：双轨 RAG + 定向微调

### 4.1 总体架构图

```
                    用户查询
                       │
                       ▼
            ┌─────────────────────┐
            │   意图路由器        │  ← 微调模型 / 轻量规则
            │  (Intent Router)    │
            └─────────────────────┘
              │              │
    术语/翻译类查询      业务流程/经验类查询
              │              │
              ▼              ▼
    ┌─────────────────┐  ┌─────────────────┐
    │   术语 RAG      │  │   邮件 RAG      │
    │ (现有 PDF 词典) │  │ (新建业务知识库)│
    │   ~12,000 条    │  │   76+ 封邮件    │
    │  结构化、精准    │  │  非结构化、经验  │
    └─────────────────┘  └─────────────────┘
              │              │
              └──────┬───────┘
                     ▼
              ┌─────────────┐
              │  答案生成器  │  ← 微调模型：学会基于检索上下文组织答案
              │  (Generator)│
              └─────────────┘
```

### 4.2 为什么不合并成一个 RAG？

| 维度 | 术语 RAG | 邮件 RAG |
|------|---------|---------|
| **数据形态** | 结构化词条（中/英/分类） | 非结构化长文本（含噪声） |
| **检索目标** | 精准匹配、Top-1 即可 | 语义相关、需要多片段综合 |
| **答案类型** | 翻译、定义 | 流程说明、经验判断、建议 |
| **更新频率** | 低（词典固定） | 高（新业务邮件不断产生） |
| **切块策略** | 整条词条为一个向量 | 需按主题/会话/段落智能切分 |

**混合存储会导致：术语查询被邮件噪声稀释，业务查询被词条片段打断。**

---

## 五、邮件 RAG 预处理与结构化流水线

不要直接使用现有的 `eml_dataset.jsonl` 入向量库。需要以下额外处理：

### 4.1 深度清洗（必做）

| 步骤 | 具体操作 |
|------|---------|
| **乱码修复** | 使用 `chardet` 或手动编码映射，重新解析 `.eml` 的 payload，优先尝试 GB2312/GBK/GB18030/UTF-8 |
| **去签名块** | 识别并移除固定格式的签名（`----`、公司名称、电话、邮箱、地址） |
| **去转发头** | 移除 `From: ... To: ... Subject: ... Sent: ...` 等转发/回复头 |
| **去纯数字表格** | 订单数量/价格表格对语义问答价值极低，可单独提取为结构化数据，不作为 RAG 文本 |
| **去 HTML 残留** | 比现有 `clean_html()` 更激进，移除 `&nbsp;`、CSS 样式残留等 |

### 4.2 智能切分：从"邮件"到"结构化事件片段"

针对**款号时间线追踪**这类场景，不能再按固定 token 或单封邮件切块。必须以 **"款号-事件"对** 为最小检索单元。

**为什么？** 一封邮件里可能同时提到 3 个款号、5 个事件，固定切块会把"Lab Dip 确认"和"出货延期"切到不同 chunk 里，导致检索召回支离破碎。

#### 推荐的事件片段格式

```json
{
  "document": "款号 INDOT27551，Lab Dip 于 2025/5/10 提交给客人，客人 5/15 确认颜色，5/16 回传工厂。原计划 5/12 提交，延迟 2 天。",
  "metadata": {
    "style_id": "INDOT27551",
    "category": "Lab Dip",
    "event_type": "客人确认",
    "date": "2025-05-15",
    "related_date": "2025-05-12",
    "delay_days": 3,
    "party_from": "客人",
    "party_to": "工厂",
    "source_email_id": "thread_001",
    "chunk_type": "event"
  }
}
```

#### 元数据字段设计（适配延期追踪场景）

| 字段 | 说明 | 示例 |
|------|------|------|
| `style_id` | 款号/订单号 | `INDOT27551`, `AW24-H5L94-00001` |
| `category` | 大阶段 | `Lab Dip`, `大货样`, `样衣`, `封样`, `面料`, `出货` |
| `event_type` | 具体事件 | `提交`, `确认`, `回传`, `出货`, `延期申请`, `给意见` |
| `date` | 事件日期（ISO 格式） | `2025-05-15` |
| `related_date` | 原计划/对比日期 | `2025-05-12` |
| `delay_days` | 延期天数 | `3` |
| `party_from` | 发起方 | `工厂`, `客人` |
| `party_to` | 接收方 | `客人`, `工厂` |
| `source_email_id` | 来源邮件 thread | `thread_001` |

#### 切分策略层级

1. **Thread 聚合**：同一 `style_id` 下的往来邮件先聚合成对话链
2. **LLM 事件提取**：用大模型从每封邮件中提取标准化事件（见 4.3）
3. **去重合并**：基于 `style_id + category + event_type + date` 去重
4. **向量化入库**：每个事件单独 embedding 为一个检索单元

### 4.3 事件提取：用 LLM 做批量结构化（关键）

7000 封邮件不能靠正则手工提取。推荐写一套 **"邮件 → 结构化事件"** 的批处理流水线：

```python
prompt = """
请从以下业务邮件中提取所有与款号、时间节点、延期相关的事件。
如果没有具体事件，返回空列表 []。

输出格式：JSON 数组，每个事件包含字段：
- style_id: 款号（必须从邮件中出现的 INDOxxx/AWxxx/SSxxx 中选）
- category: 业务阶段
- event_type: 事件类型
- date: 日期（YYYY-MM-DD，尽量从上下文推断绝对日期）
- related_date: 原计划日期（如有）
- delay_days: 延期天数（数字，延期为正，提前为负）
- description: 一句话描述
- confidence: 提取置信度（0~1）

邮件内容：
{email_content}
"""
```

**质量控制措施**：
| 问题 | 解决方案 |
|------|---------|
| LLM 编造假款号 | 预先用正则提取邮件中所有真实款号，强制 LLM 只能从列表中选择 |
| 日期格式混乱 | 预处理阶段将相对日期（"本月10号"）根据邮件发送时间推算为绝对日期 |
| 事件重复 | 基于复合键 `style_id+category+event_type+date` 去重 |
| 低置信度 | 只保留 `confidence >= 0.7` 的事件，低于阈值的人工复核或归档 |

### 4.4 检索增强策略

| 策略 | 说明 |
|------|------|
| **嵌入模型** | 继续使用 `BAAI/bge-large-zh-v1.5` 或升级到 `BAAI/bge-m3` |
| **款号精确过滤** | 先用 `where={"style_id": "INDOT27551"}` 精确过滤，再在该款号范围内做向量语义检索 |
| **时间衰减排序** | 优先返回近 1 年/6 个月的邮件，旧邮件降低权重 |
| **两阶段检索** | 阶段1：按 `style_id` 元数据召回所有相关事件；阶段2：按 `category` + `date` 排序聚合为时间线 |
| **置信度阈值** | 设置最低相似度阈值（如 0.65），低于阈值时回答"未找到相关邮件记录" |

### 4.4 数据量建议

- **当前 7000 封**：数据量充足，足以支撑中高覆盖度的业务知识库
- **预估有效记录**：去重筛选后约 3500~5000 封，按每封提取 1~3 个事件，最终约 **1~2 万个事件片段**
- **ChromaDB 适用性**：1~2 万条向量在 ChromaDB 中查询仍处毫秒级，短期内无需迁移 ES/Milvus
- **补充来源**：可将 SOP 文档、合同模板、质检报告等业务文档一并入库，进一步增强覆盖面

---

## 五、微调方向需要如何调整？

### 5.1 当前微调的问题

现有微调数据（`eml_dataset.jsonl` → `chatml_train.jsonl`）本质上是让模型学习"**怎么写邮件**"或"**邮件分类**"，存在以下不足：

1. **没有构造问答对**：模型没有学会"回答邮件相关问题"
2. **数据量小且重复**：旧流程基于 121 封产生 250 条上采样数据，多样性不足。新数据量下需重新构造**
3. **没有利用 RAG 检索结果**：模型无法学会"基于外部知识生成答案"
4. **缺乏结构化推理能力**：现有数据未训练"跨邮件时间线聚合"和"表格化根因分析"能力

### 5.2 推荐的四个微调方向

#### 方向 A：意图路由器（最推荐，轻量）

**目标**：让模型判断用户查询应该调用哪个工具/RAG。

**训练数据示例（ChatML）**：
```json
[
  {"role": "system", "content": "你是一个服装行业AI助手，请判断用户问题应该查询哪个知识库。"},
  {"role": "user", "content": "拉链的英文怎么说？"},
  {"role": "assistant", "content": "[TOOL: terminology_rag] 查询: 拉链"}
]
[
  {"role": "user", "content": "ASOS 订单核料一般要多久？"},
  {"role": "assistant", "content": "[TOOL: email_rag] 查询: ASOS 核料 交期"}
]
[
  {"role": "user", "content": "帮我写一封确认 Lab Dip 的邮件"},
  {"role": "assistant", "content": "[TOOL: generate_email] 主题: Lab Dip 确认"}
]
```

**适用场景**：作为入口层，快速分流用户请求。

---

#### 方向 B：RAG 增强生成（RAG Fine-tuning）

**目标**：让模型学会基于检索到的邮件片段/术语，组织准确、连贯的答案。

**训练数据示例（Alpaca/ShareGPT）**：
```json
{
  "instruction": "根据检索到的业务邮件记录，回答关于 ASOS 核料流程的问题。",
  "input": "[文档1] 2025/5/15，INDOT27551 核料确认，主面料 PVE TF23149... [文档2] 2025/6/9，INDOT24846 V2 大货面料订单，Chestnut 色...",
  "output": "根据邮件记录，ASOS 核料流程一般需要先确认 Lab Dip 颜色，然后核对主面料和里料的数量及单价，最终生成大货面料订单。以 INDOT27551 为例，主面料为 PVE TF23149，里料为 210T 斜纹布..."
}
```

**核心收益**：模型不会 hallucinate（编造）未在检索结果中出现的信息。

---

#### 方向 C：邮件回复助手（终极目标）

**目标**：输入incoming邮件内容 + RAG检索到的术语/历史邮件，输出专业回复草稿。

**训练数据示例**：
```json
{
  "messages": [
    {"role": "system", "content": "你是服装行业业务助理。请根据客户邮件和检索到的相关知识，撰写一封专业回复。"},
    {"role": "user", "content": "客户邮件：关于 INDOB29034 的核料确认，请确认主面料 Cotton Twill 的 Lab Dip 颜色，并安排 invisible zipper 5号规格。\n\n[术语检索] invisible zipper → 隐形拉链 (分类: 拉链)\n[历史邮件] 类似订单 ASOS 核料通常需要 3-5 个工作日确认 Lab Dip。"},
    {"role": "assistant", "content": "Subject: RE: INDOB29034 核料确认\n\nHi [Client Name],\n\nThanks for your email. Please see below confirmation for style INDOB29034:\n\n1. Shell fabric: Cotton Twill - Lab Dip color confirmation is in progress, estimated 3-5 working days.\n2. Invisible zipper (5# gauge) has been arranged as requested.\n\nWe will update you once Lab Dip is approved.\n\nBest regards,\n[Your Name]"}
  ]
}
```

---

#### 方向 D：时间线推理与根因分析（针对延期追踪场景）

**目标**：让模型学会基于检索到的事件列表，生成结构化表格并分析延期根因。

**训练数据示例（ChatML）**：
```json
[
  {"role": "system", "content": "你是服装行业业务分析师。请根据以下事件记录，整理成时间线表格并分析延期根因。如果某阶段无记录，请明确标注'未找到记录'，不要编造。"},
  {"role": "user", "content": "款号 INDOT27551 的事件记录：\n[事件1] Lab Dip 工厂提交: 2025-05-10, 原计划 2025-05-08\n[事件2] Lab Dip 客人确认: 2025-05-15, 原计划 2025-05-12\n[事件3] 出货延期申请: 从 2025-06-25 延至 2025-07-05\n[事件4] 样衣提交: 未找到记录"},
  {"role": "assistant", "content": "（输出结构化 Markdown 表格 + 根因分析，参见第三章理想输出示例）"}
]
```

**核心收益**：模型能稳定输出可预期的表格格式，并具备"对比计划 vs 实际"的推理能力。

---

### 5.3 微调策略优先级

| 优先级 | 方向 | 所需数据量 | 难度 | 收益 |
|--------|------|-----------|------|------|
| P0 | 意图路由器 | 200+ 条 | 低 | 让系统从"单一体"变成"可扩展架构" |
| P1 | 时间线推理与根因分析 | 300+ 条 | 中 | **直接解决款号延期追踪核心场景**，输出结构化表格 |
| P2 | RAG 增强生成 | 300+ 条 | 中 | 显著提升一般业务问答的准确性和可信度 |
| P3 | 邮件回复助手 | 500+ 条 | 高 | 最终实现端到端的邮件自动化 |

---

## 六、下一步行动清单

### 阶段 1：数据清洗与款号提取（已完成 ✅）
- [x] 编写**批量清洗脚本**（处理 7359 封）：`pre_filter_emails.py` 已筛选出 2571 封有效邮件
- [x] 建立**款号正则库**：覆盖 `CCSS/INDO/AW/SS/CDIB/P_SERIES/7位数字` 7 种格式
- [x] 按 `style_id` 聚合同一款号下的往来邮件
- [x] 提取并隔离纯数字订单表格（存为结构化 JSON，不入 RAG）

### 阶段 2：LLM 结构化事件提取（已完成 ✅）
- [x] 设计**事件提取 Prompt**（含字段约束和 JSON Schema）
- [x] 抽取 100 封样本做**试点提取**，人工评估准确率（目标 >85%）
- [x] 优化 Prompt 和日期推算逻辑后，**批量跑完全部 2571 封有效邮件**
- [x] 质量过滤：4668 条 passed（94.2%），163 条 review，126 条 discarded
- [x] 基于 `style_id+category+event_type+date` 做事件去重

### 阶段 3：构建邮件事件 RAG（已完成 ✅）
- [x] 新建 ChromaDB collection：`email_events`
- [x] 每个事件片段单独 embedding，携带完整元数据（`style_id`, `category`, `event_type`, `date`, `delay_days`）
- [x] 实现**两阶段检索**：`where={"style_id": "xxx"}` 精确过滤 + 向量语义排序 + 按 `category/date` 聚合
- [x] 端到端检索验证脚本 `test_timeline_rag.py` 已可运行

### 阶段 4：微调数据构造与训练（进行中 🔄）
- [ ] 构造 **意图识别数据集** 200+ 条（必须包含"款号时间线查询"类别）
- [x] 构造 **时间线推理数据集** 300+ 条：黄金样本 221 条已生成（`email_finetuning_pipeline/datasets/timeline_gold_samples.jsonl`），待 LLM 增强扩增至 300+
- [ ] 使用 LLaMA-Factory + LoRA 进行两阶段训练：先训意图路由（1 epoch），再训时间线推理（2-3 epochs）

### 阶段 5：系统集成与端到端验证（2-3 天）
- [ ] 实现 `IntentRouter`：识别到款号即路由至 `email_rag_timeline`
- [ ] 封装 `TimelineAnalyzer`：接收 `style_id`，调用两阶段检索，组装事件列表进 Prompt
- [ ] 将术语 RAG、邮件事件 RAG、生成模块统一封装为 `EmailAssistant` API
- [ ] 端到端测试：用 5~10 个真实款号验证"检索完整性 → 表格生成 → 根因分析"的链路

---

## 七、关键决策点（已更新）

基于最新信息，以下问题已得到确认。

### 7.1 已确认事项

| 问题 | 当前判断 |
|------|---------|
| **数据规模** | **7359 封原始邮件**，数据量充足，邮件 RAG 具备高价值 |
| **核心场景** | **款号延期根因追踪**（第三章示例）。属于"结构化时间线推理"，术语 RAG 无法替代，必须依赖邮件事件 RAG + 定向微调 |
| **LLM 渠道** | 选择**调用 API** 进行批量结构化提取 |
| **人工复核** | 由用户负责复核，脚本需自动将 `confidence < 0.7` 的记录导出为待复核清单 |
| **输出格式** | Markdown 表格符合预期，暂不接入外部系统。可基于数据特征进一步扩展子场景 |

### 7.2 款号/订单号格式覆盖分析结果

#### 数据来源
对全部 **7359 封邮件**的主题进行准确解码分析（使用 `email.header.decode_header` 处理 base64/QP 编码）。

#### 核心发现
- **含款号邮件**：**530 封（7.2%）**
- **唯一款号数量**：**85 个**
- **主题类型分布**：original 3459 封 / reply 3389 封 / forward 511 封

#### 款号前缀分布（按主题出现次数）
| 前缀 | 出现次数 | 占比 | 说明 |
|------|---------|------|------|
| **INDO** | 213 | 40.2% | 主要款号体系，如 `INDOT26919`、`INDOB26835` |
| **CCSS** | 200 | 37.7% | 重要款号体系，如 `CCSS230011`、`CCSS240158` |
| **AW** | 166 | 31.3% | 季节+类别编码，如 `AW25-KFWTS101` |
| **SS** | 18 | 3.4% | 较少，如 `SS25IACB201` |
| **P_SERIES** | 5 | 0.9% | 面料编号，非款号但常与款号共存，如 `P009-H5L94-00001` |
| **CDIB** | 4 | 0.8% | 极少，如 `CDIB18384` |

> 注：一封邮件主题可能同时出现多个款号/编号，因此前缀占比之和 > 100%。

#### 高频率款号 Top 10
1. `INDOJ915`: 38 次
2. `AW25-KFWTS101`: 37 次
3. `AW25-KFWTT156`: 28 次
4. `INDOT23404`: 23 次
5. `AW25-KFCWT147`: 23 次
6. `INDOT22861`: 20 次
7. `INDOT24846`: 20 次
8. `INDOB28113`: 19 次
9. `SS25IACB201`: 18 次
10. `AW25-KFCWT149`: 18 次

#### 完善的正则规则

```python
STYLE_ID_PATTERNS = {
    # CCSS: 5-6位数字（如 CCSS23001, CCSS230011）
    'CCSS': r'\bCCSS\d{5,6}\b',
    
    # INDO: 1位大写字母 + 3-6位数字（如 INDOJ915, INDOT23404）
    'INDO': r'\bINDO[A-Z]\d{3,6}\b',
    
    # AW: 2位数字-大写字母组合-数字（如 AW25-KFWTS101）
    'AW': r'\bAW\d{2}-[A-Z]+\d+\b',
    
    # SS: 2位数字 + 大写字母 + 数字（如 SS25IACB201）
    'SS': r'\bSS\d{2}[A-Z]+\d+\b',
    
    # CDIB: 数字后缀（如 CDIB18384）
    'CDIB': r'\bCDIB\d+\b',
    
    # P-系列: 面料编号（如 P009-H5L94-00001）
    'P_SERIES': r'\bP\d{3}-[A-Z0-9]+-\d+\b',
}
```

#### 对数据策略的影响
- **7.2% 的邮件主题含款号** → 约 530 封邮件是"款号时间线追踪"的核心数据源
- 不含款号的邮件（约 93%）主要是日常沟通、通用通知，对特定款号追踪价值较低，可作为低优先级保留或过滤
- 每封含款号邮件平均可提取 **2~3 个事件**，预估最终事件库规模约 **1000~1500 条**，完全可用
- **必须从解码后的文本中提取款号**，绝不能直接用 `grep` 原始 `.eml` 二进制内容（UTF-8 Q-encoding 会导致截断和误匹配）

### 7.3 可扩展的子场景（基于数据特征）

除"延期根因追踪"外，基于现有 7000 封邮件的数据特征，还可延伸以下子场景：

| 子场景 | 示例提问 | 所需能力 |
|--------|---------|---------|
| **款号状态速查** | "INDOT23404 现在到哪个阶段了？" | 按 `style_id` 检索最新事件，输出当前状态 |
| **阶段历史汇总** | "CCSS230011 的 Lab Dip 确认了几次？" | 按 `style_id + category` 聚合，统计事件次数 |
| **客户反馈追踪** | "客人对 AW25-KFWTS101 的样衣有什么意见？" | 按 `style_id + event_type=给意见` 检索并汇总 |
| **面料变更记录** | "P009-H5L94-00001 这个面料配过哪些款号？" | 按 `P_SERIES` 反查关联的 `style_id` |
| **高频问题款号** | "过去一年哪个款号延期次数最多？" | 基于 `delay_days > 0` 做聚合统计 |

这些子场景共享同一套"事件型邮件 RAG"基础设施，只需在检索过滤条件和输出模板上做微调即可。

---

*文档生成时间：2026-04-14*  
*建议每取得一个阶段性进展后，回来更新此文档的"当前现状"和"行动清单"部分。*
