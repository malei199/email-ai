# 记忆系统与多轮对话设计（参考文档）

> 创建日期：2026-05-01
> 状态：技术讨论已确认，待后续实现
> 相关文档：V3_ROUTING_RULES.md (v2.0), V3_ROUTE_V1_IMPLEMENTATION_PLAN.md (v2.0)

---

## 一、设计原则

1. **V3 保持纯粹**：V3 只负责单轮路由决策，不学指代消解、不维护记忆
2. **分层处理**：记忆/指代/续查在 V3 之前的预处理层解决
3. **续查绕过 V3**：明确的续查意图直接查记忆返回，不经过 V3
4. **Query Rewrite 补全指代**：有指代/省略的问题，在进 V3 前改写为完整问题

---

## 二、三层架构

```
用户输入
  ↓
【Layer 1: 预处理层】（V3 之前）
  ├── 续查检测 → 查记忆 → 直接返回（绕过 V3）
  ├── 指代消解/省略补全 → Query Rewrite → 补全后进入 Layer 2
  └── 多意图冲突检测 → 直接返回提示（绕过 V3）
  ↓
【Layer 2: V3 路由层】
  ├── 有款号 → route_v1
  ├── 无款号 → route_v2
  └── 其他 → direct_answer
  ↓
【Layer 3: 执行层】（V3 之后）
  ├── route_v1 → 外部系统调 V1
  ├── route_v2 → V2 引擎自治查询
  └── direct_answer → 直接返回
```

---

## 三、Layer 1 详细设计

### 3.1 续查检测（绕过 V3）

**触发关键词**：
```
"剩下的", "下一页", "还有呢", "更多", "接着", "继续",
"第.*个", "详细点", "具体点", "展开说说"
```

**处理流程**：
```
用户: "剩下的呢"
  ↓
检测续查意图 ✅
  ↓
查 Redis/DB 记忆（session_id）
  ↓
获取上次查询状态（page=1, has_more=true, total=15）
  ↓
返回第 2 页结果（6-10/15）
  ↓
更新记忆（page=2）
  ↓
直接返回给用户（V3 不参与）
```

**记忆存储结构**：
```json
{
  "session_id": "uuid",
  "last_query": "我跟的款号有哪些",
  "last_response_type": "style_list",
  "styles": ["CCSS230021", "CCSS230022", ...],
  "page": 1,
  "page_size": 5,
  "total": 15,
  "has_more": true,
  "timestamp": "2026-05-01T10:00:00Z"
}
```

---

### 3.2 Query Rewrite（指代消解 + 省略补全）

**核心思想**：将多轮对话中的当前问题，结合历史上下文改写成完整的单轮问题。

**业界方案**：
- **LLM-based**（主流）：用轻量 LLM（3B-7B）做指代消解和省略补全
- **规则-based**：关键词匹配 + 模板替换（简单场景）
- **混合**：规则先做快速匹配，LLM 处理复杂 case

**改写示例**：

| 历史对话 | 当前问题 | 改写后 |
|---------|---------|--------|
| Q: CCSS230021 的时间线 | 这个款号怎么样了 | CCSS230021 怎么样了 |
| Q: 我跟的款号有哪些<br>A: 3个：021, 022, 023 | 这几个款号哪个更有风险 | CCSS230021, CCSS230022, CCSS230023 哪个更有风险 |
| Q: 如何办理社保卡 | 需要哪些材料 | 办理社保卡需要哪些材料 |

**Query Rewrite Prompt 模板**：
```
请根据历史对话，对当前问题进行指代消解和省略补全。
如果当前问题完整（无指代/省略），直接返回原问题。

历史对话：
{history}

当前问题：{current_question}

请输出 JSON 格式：
{"rewritten": "改写后的问题", "need_rewrite": true/false}
```

**实现建议**：
- 使用轻量模型（如 Qwen2.5-3B-Instruct）
- 或调用外部 LLM API（成本低，延迟可接受）
- 缓存改写结果，避免重复调用

---

### 3.3 多意图冲突检测（绕过 V3）

**场景**：用户问题同时包含有款号和无款号意图

**示例**：
```
"CCSS230021 的信息，顺便帮我看看 Paula Cheng 的所有款号"
```

**处理**：
```
检测到有款号 + 无款号混合意图
  ↓
直接返回提示（绕过 V3）：
"您的问题包含多个意图（有款号查询+无款号查询），请拆分后分别提问。"
```

---

## 四、各层职责划分

| 层级 | 职责 | 是否绕过 V3 |
|-----|------|-----------|
| **Layer 1** | 续查检测、Query Rewrite、多意图过滤、记忆管理 | 续查/多意图时 ✅ 绕过 |
| **Layer 2 (V3)** | 纯路由决策（3 种） | - |
| **Layer 3** | V1/V2 调用、结果展示 | - |

---

## 五、V3 训练数据影响

**V3 不需要学**：
- ❌ 指代消解
- ❌ 续查处理
- ❌ 记忆管理
- ❌ 多轮对话历史

**V3 保持**：
- ✅ 单轮路由决策（system + user + assistant，3 条 message）

---

## 六、业界参考

### 6.1 Query Rewrite 方案

| 方案 | 来源 | 说明 |
|-----|------|------|
| **Utterance Rewriter** | 腾讯微信 AI (ACL 2019) | 基于 Transformer + 指针网络，中文多轮对话改写 |
| **MultiQueryRetriever** | LangChain | 多查询视角增强检索，可参考实现 |
| **GPT-4 Zero-shot** | OpenAI | 通过指令模板实现零样本指代消解 |
| **基于 Prompt 的改写** | 业界通用 | 用 LLM 直接做指代判断 + 改写 |

### 6.2 关键论文

1. Su et al. (2019) - "Improving Multi-turn Dialogue Modelling with Utterance ReWriter"
   - 提出话语重写解决指代消解和信息省略
   - 基于 Transformer + 指针网络

2. Zhou et al. - 利用改写帮助上下文建模
   - 将多轮对话输入改写为单轮对话输入

### 6.3 指代类型与解决方案

| 指代类型 | 案例 | 解决技术 |
|---------|------|---------|
| 人称代词 | "他/她/它" | 实体聚类、LLM 上下文理解 |
| 指示代词 | "这/这些/那个" | Few-shot Prompt、语义相似度 |
| 时间指代 | "这一年/当时" | 时间实体映射 |
| 零指代（省略） | "需要材料？" | 序列标注、语义补全、LLM 推理 |
| 序数指代 | "第3个" | 记忆状态追踪 |

---

## 七、待实现清单

- [ ] Layer 1 预处理模块（续查检测 + Query Rewrite + 多意图过滤）
- [ ] 记忆存储系统（Redis/DB，session 级别）
- [ ] Query Rewrite 模型选型（轻量 LLM 或规则）
- [ ] 记忆状态定义（page、has_more、styles 等）
- [ ] 续查关键词库维护
- [ ] 与 V3 的集成测试

---

## 八、参考文档

- `V3_ROUTING_RULES.md` (v2.0) — V3 路由规则
- `V3_ROUTE_V1_IMPLEMENTATION_PLAN.md` (v2.0) — V1 链路实现
- `build_v3_scheduler_dataset.py` — V3 训练数据生成

---

*文档创建日期：2026-05-01*
